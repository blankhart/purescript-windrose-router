{
  "name": "purescript-servant-routing",
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build:example": "pulp build --include example --main Example.Main --to example/main.js",
    "run:example": "yarn build:example && parcel example/index.html"    
  },
  "devDependencies": {
    "bower": "^1.8.2",
    "inferno": "^6.2.1",
    "inferno-vnode-flags": "^6.2.1",
    "parcel-bundler": "1.12.3",
    "pulp": "^12.2.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.6.0"
  },
  "dependencies": {
    "purs": "^0.3.0"
  }
}
